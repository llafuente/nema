swagger: '2.0'
info:
  title: RestManagementAPI
  description: These APIs allow to handle Algo Framework
  version: 1.2.0
  contact:
    name: XXX
    email: YYY@ZZZ.com
    url: https://abc.com
host: localhost:8080

x-nema:
  moduleName: AutoHedgerApiModule
  className: RestApi

schemes:
  - http
  - https
# basePath: /api/v1
consumes:
  - application/json
produces:
  - application/json



parameters:
  stringQ:
    name: stringQuery
    in: query
    description: This is a string query
    required: true
    type: string
  dateQ:
    name: dateQuery
    in: query
    description: This is a date query
    required: true
    type: string
    format: date
  arrayQ:
    name: arrayQuery
    in: query
    description: This is a array of strings query
    required: true
    type: array
    items:
      type: string

  numberQ:
    name: numberQuery
    in: query
    description: This is a number query
    required: true
    type: number
    format: int32
  stringH:
    name: stringHeader
    in: header
    description: This is a string Header
    required: true
    type: string
  dateH:
    name: dateHeader
    in: header
    description: This is a date Header
    required: true
    type: string
    format: date
  arrayH:
    name: arrayHeader
    in: header
    description: This is a array of strings Header
    required: true
    type: array
    items:
      type: string
  numberH:
    name: numberHeader
    in: header
    description: This is a number Header
    required: true
    type: number
    format: int32

  UserBody:
    in: body
    name: user
    required: true
    schema:
      $ref: '#/definitions/UserDto'
  userId:
    name: userId
    in: path
    description: This is a number Path
    required: true
    type: number
    format: int32

definitions:
  ApiError:
    description: Api error
    type: object
    properties:
      message:
        type: string

  UserDto:
    type: object
    properties:
      id:
        type: number
      name:
        type: string

  string:
    type: string

  stringB:
    description: This is a string Body
    type: string

  dateB:
    description: This is a date Body
    type: string
    format: date
  arrayB:
    description: This is a array of strings Body
    type: array
    items:
      type: string
  numberB:
    description: This is a number Body
    type: number
    format: int32

responses:
  ErrorResponse:
    description: The specified resource was not found
    schema:
      $ref: '#/definitions/ApiError'

paths:
  /user/{userId}:
    parameters:
      - $ref: "#/parameters/userId"
    get:
      operationId: getUserById
      description: get User
      parameters:

        - $ref: "#/parameters/stringQ"
        - $ref: "#/parameters/dateQ"
        - $ref: "#/parameters/arrayQ"
        - $ref: "#/parameters/numberQ"
        - $ref: "#/parameters/stringH"
        - $ref: "#/parameters/dateH"
        - $ref: "#/parameters/arrayH"
        - $ref: "#/parameters/numberH"

      responses:
        200:
          description: user data
          schema:
            $ref: '#/definitions/UserDto'
        500:
          $ref: '#/responses/ErrorResponse'
    post:
      operationId: updateUserById
      description: upate User
      parameters:
        - $ref: "#/parameters/UserBody"

      responses:
        200:
          description: The identifier of the created strategy
          schema:
            $ref: '#/definitions/UserDto'
        500:
          $ref: '#/responses/ErrorResponse'
